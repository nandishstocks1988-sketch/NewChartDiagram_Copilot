export const SystemWizard = {
  init: function() {
    // Render system creation wizard/popover/modal
    // Stepwise form: name, font style, color, layout, order, background
    // Attach to #wizard element
  }
}