export const LegendPanel = {
  init: function() {
    // Generate legend from diagram data
    // Allow placement in diagram and ensure export includes legend
    // Attach to #legend-panel element
  }
}